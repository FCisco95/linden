{
  "name": "linden",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension that scores CVs against a JD. Never auto-rejects. BYO key, zero backend.",
  "license": "MIT",
  "author": "João Francisco Vieira",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval": "tsx scripts/eval.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
